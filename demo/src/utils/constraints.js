const constraints = {
    email: {
        presence: {
            message: 'Enter Email'
        },
        email: {
            message: 'is not valid'
        }
    }
};

export default constraints;